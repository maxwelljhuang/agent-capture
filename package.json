{
  "name": "agent-capture-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Trajectory capture engine for AI agent compliance",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "schema:generate-types": "./scripts/generate_ts_types.sh"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.19.41",
    "js-yaml": "^4.1.1",
    "json-schema-to-typescript": "^15.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=18"
  }
}
