"""Multi-component integration tests (Weeks 3-4).

These tests exercise the full pipeline (instrumentation → builder →
redaction → exporter) and require optional extras. Mark with
``@pytest.mark.integration`` so unit-only runs can skip them.
"""
